<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <!--Brand and Image -->
    <a class="navbar-brand text-danger" href="<%= root_path %>">
        <!--<img src="/docs/4.3/assets/brand/bootstrap-solid.svg" width="30" height="30" class="d-inline-block align-top" alt="">-->
        <i class="fab fa-pinterest"></i>
        Pinterest
    </a>
  
    <!--Collapse Toggler -->
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    
    <!-- Start Nav-->
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href= "<%= root_path %>"> Inicio<span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= pages_about_path %>">Nosotros</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= pages_contact_path %>">Contacto</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= pins_path %>">Pins</a>
      </li>
    </ul>

    <form class="form-inline mr-4 align-middl">
        <!-- Nombre de Usuario -->
        <%# <img src="/docs/4.3/assets/brand/bootstrap-solid.svg" width="30" height="30" class="d-inline-block align-top" alt=""> %>

        <%  unless user_signed_in? %>
          
            <%= link_to "Registrate", new_user_registration_path, class: "btn btn-outline-primary btn-sm mr-2", role: "button" %>
          
            <%= link_to "Tu Cuenta", user_session_path, class: "btn btn-outline-secondary btn-sm mr-2", role: "button" %>
          
        <% else %>

          <span class="navbar-text mr-2 align-middle">
            <% if current_user.avatar != nil %>
              <%= image_tag(current_user.avatar, class:'img-fluid rounded-circle align-middle', style: 'max-width: 2.1rem') %>
            <% else %>
              <i class="far fa-user-circle mr-2"></i>
            <% end %>
            Usuario: <%= current_user.name %> | <%= current_user.email %>
          </span>

            <%= link_to "Cerrar Sesión", destroy_user_session_path, method: :delete, class: "btn btn-outline-danger btn-sm mr-2", role: "button" %>

        <% end %>
        
    </form>

    <%  if user_signed_in? %>
    <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2 btn-sm" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success my-2 my-sm-0 btn-sm" type="submit">Search</button>
    </form>
    <% end %>
  </div>
</nav>